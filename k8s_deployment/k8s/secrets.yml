apiVersion: v1                          # Core API version.
kind: Secret                            # Declares a Kubernetes Secret resource.
metadata:
  name: db-secrets                      # Name of the secret object.
type: Opaque                            # Generic secret type (base64-encoded key-value).
data:
  db-password: bXlTM2tVcjNQYXNzd29yZDMx # Base64-encoded password; decode with `echo ... | base64 -d`
# echo -n 'your-password' | base64
