.qodo

# Local .terraform directories
**/.terraform/*
.terraform/
*.tfstate
*.tfstate.backup

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.*

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore everything ending with .tfplan
*.tfplan

